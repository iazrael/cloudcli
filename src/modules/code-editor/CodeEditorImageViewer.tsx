import {
  ImageViewerSurface,
  MAX_SCALE,
  MIN_SCALE,
  type ImageViewerHandle,
} from '@/shared/ui/ImageViewerSurface';

export { MAX_SCALE, MIN_SCALE, type ImageViewerHandle };
export default ImageViewerSurface;
