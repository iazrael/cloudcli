# CloudCLI 会话上下文

此上下文定义 CloudCLI 在提供方会话与聊天记录之间使用的稳定术语，避免不同提供方的实时数据含义混淆。

## Language

**ZCode 实时会话事件**:
ZCode 引擎在一个活跃会话运行期间发出的增量事实，包括文本、推理、工具调用、权限请求、完成和失败。它不同于稍后从 ZCode 本地记录读取的历史记录。
_Avoid_: ZCode 消息、流数据、实时记录

**ZCode 实时会话状态**:
一个活跃 ZCode 会话中尚未闭合的推理段和工具调用信息。它只属于该会话，并在该会话最终结束时失效。
_Avoid_: 全局流状态、跨会话上下文

**ZCode 运行生命周期**:
一次运行从启动到终态的全部状态：完成/失败判定、静默看门狗时间戳、token 聚合、权限桥 writer 映射。一次运行只有一个归属者（运行协程或后台静默守望者），终态处理与清理随归属者走。
_Avoid_: 散落的活跃会话表、多处清理逻辑
